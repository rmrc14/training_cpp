#include<iostream>

using namespace std;

void normalize_sentence(char* str)
{
	int length = sizeof(str);
	cout << length;
}
int main()
{
	char ch[10] = "hello";
	

	normalize_sentence(ch);
	
	return 0;
}