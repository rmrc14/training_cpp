#include <iostream>
#include <set>
#include <string>

using namespace std;

//bool startsWith(const string& word, const string& prefix) {
//    return word.substr(0, prefix.size()) == prefix;
//}
//
//int main() {
//    set<string> dictionary = { "apple", "application", "banana", "apply", "ball" };
//    string prefix = "app";
//
//    auto it = dictionary.lower_bound(prefix);
//    while (it != dictionary.end() && startsWith(*it, prefix)) {
//        cout << *it << endl;
//        ++it;
//    }
//
//    return 0;
//}
